pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh "echo 'build JAR'"
            }
        }
        stage('deploy') {
            steps {
                sh 'echo "deploy JAR"'
            }
        }
    }
}

